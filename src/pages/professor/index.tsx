import { FC } from 'react';
import { TeacherList as TeacherListComponent } from '@components/Pages/Professor/Index';

const TeacherList: FC = () => <TeacherListComponent />;

export default TeacherList;
