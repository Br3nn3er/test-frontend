import { FC } from 'react';

import { CreateTeacher as CreateTeacherComponent } from '@components/Pages/Professor/Novo';

const CreateTeacher: FC = () => <CreateTeacherComponent />;

export default CreateTeacher;
